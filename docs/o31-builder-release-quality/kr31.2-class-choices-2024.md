# KR31.2 — Класові й підкласові вибори 2024

**Ціль:** [O31](README.md) · **Статус:** ✅ зроблено 2026-09-06 · **Залежить від:** [KR31.1](kr31.1-edition-end-to-end.md)

## Мета

Гравець 2024 робить у майстрі кожен вибір, який дає йому книга на цьому рівні, і вибір щось змінює в персонажі.

## Виміряно 2026-09-04

Виміряно запитами до `spells_test` і оглядом конструктора в браузері:

- `class_choice_option` під `RULES_2024` порожня для дев'яти класів із тринадцяти. Немає
  Божественного ордену клірика, Первісного ордену друїда, Метамагії чародія (і рядка риси на 10 і
  17 рівнях), Первісного знання варвара, опцій `Blessed Warrior` паладина й `Druidic Warrior`
  слідопита.
- `subclass_choice_option` під `RULES_2024` порожня **цілком** — жоден із 48 підкласів не має
  жодного вибору, тож крок `subclass-choices` для 2024 не з'явиться ніколи.
- `class.skill_proficiencies` дорівнює `NULL` в **усіх тринадцяти** класах 2024: крок «Навички» не
  пропонує класового вибору жодному персонажу 2024. Те саме з інструментами.
- Експертизи 2024 немає: `skill_expertises` порожнє в розбійника, барда, слідопита й чарівника, а
  других надань (розбійник 6, бард 9) немає навіть рядком.
- Крок «Опції риси походження» не показується **жодному** з 16 походжень 2024, тож риса
  походження з виборами (Посвячений у магію, Умілець) створюється порожньою.
- `ARTIFICER_2024` доступний у конструкторі й має нуль підкласів.

## Що зробити

1. Скласти з `data/2024/srd/classes.md` і `data/2024/normalized/*.json` перелік усіх виборів
   1–20 рівнів за класом і підкласом, з кількістю виборів на рівень.
2. Сід під `RULES_2024` у `class_choice_option`, `subclass_choice_option`, `skill_proficiencies`,
   `tool_proficiencies` — правкою **файлів-джерел** ([Р33](../DECISIONS.md#р33)), не проходом по базі.
3. Крок «Опції риси походження» в конструкторі 2024 (умова вже є в `creation-step-resolver`, її
   ніхто не вмикає для походжень).
4. Гейт: тест звіряє кількість виборів кожного класу 2024 на кожному рівні з книгою.

## Готово, коли

- [x] Кожен клас 2024 на 1-му рівні пропонує рівно ті вибори, що в книзі, і вони зберігаються.
- [x] Підкласи 2024 мають свої вибори; Коло землі, Здобич мисливця, Стихійна спорідненість працюють.
      **Межа:** покрито чотири підкласи, названі цим KR (плюс Захисна тактика мисливця); решта
      підкласів із власними виборами — Armorer, Arcane Archer, Rune Knight, Four Elements — лишається
      незакритою й описана нижче в журналі.
- [x] Навички та інструменти класу 2024 обираються в конструкторі.
- [x] Експертиза 2024 працює в розбійника, барда, слідопита й чарівника.
- [x] Рішення власника по Артифайсеру виконано.

## Знахідки аудиту (48)

Повний доказ кожної — у [findings.md](findings.md), сирий звіт автора — у [audit/](audit/).

| Рівень | Ред. | Тип | ID | Що не так |
|---|---|---|---|---|
| P0 | 2024 | bug | `L04-class-features-08` | ARTIFICER_2024 має 0 підкласів, але доступний у каталозі й конструкторі: персонаж проходить 3-й рівень із subclassId = null без жодної помилки і назавжди лишається без підкласу |
| P1 | 2024 | data | `L01-species-05` | Людська «Вправність» (володіння однією навичкою на вибір) не реалізована ніде — ні в даних, ні в коді |
| P1 | 2024 | bug | `L02-backgrounds-01` | Конструктор 2024 ніколи не показує крок «Опції риси походження», тож риса походження з виборами (Посвячений у магію, Умілець) створюється порожньою — 6 із 16 походжень |
| P1 | 2024 | data | `L02-backgrounds-02` | Походження не фіксує список заклинань «Посвяченого у магію»: книга дає Magic Initiate (Cleric/Wizard/Druid), база — одну рису з вільним вибором списку |
| P1 | 2024 | data | `L04-class-features-02` | Експертизи в 2024 немає взагалі: другий грант (розбійник 6, бард 9) відсутній у базі, а сама риса не несе вибору навичок, тож крок «Експертиза» ніколи не показується |
| P1 | 2024 | data | `L04-class-features-03` | Метамагія чародія 2024: рядок риси лише на 2-му рівні (немає на 10 і 17), а опцій метамагії для RULES_2024 у базі немає жодної — вибору гравець не робить ніколи |
| P1 | 2024 | data | `L04-class-features-06` | Divine Order (клірик 1) і Primal Order (друїд 1) подані однією пасивною рисою без вибору — гравець не обирає роль і не отримує важкий/середній обладунок та військову зброю, що вона дає |
| P1 | 2024 | data | `L04-class-features-07` | Barbarian Primal Knowledge (3-й рівень) не дає навички і не пропонує вибору |
| P1 | 2024 | missing-system | `L05-class-choices-01` | Клірик 2024 ніколи не обирає Divine Order (Захисник / Дивотворець) — крок вибору не існує ні в конструкторі, ні деінде |
| P1 | 2024 | missing-system | `L05-class-choices-02` | Друїд 2024 ніколи не обирає Primal Order (Magician / Warden) |
| P1 | 2024 | data | `L05-class-choices-03` | Друїд 2024 отримує середні обладунки безумовно, хоча книга дає їх лише через Primal Order → Warden |
| P1 | 2024 | missing-system | `L05-class-choices-04` | Чародій 2024 не має Метамагії взагалі — ні опцій у базі, ні правила кількості виборів |
| P1 | 2024 | data | `L05-class-choices-05` | Експертиза 2024 не працює в жодного класу: skill_expertises порожнє в Rogue/Bard/Ranger/Wizard, а других надань (Rogue 6, Bard 9) немає навіть як рядків |
| P1 | 2024 | missing-system | `L05-class-choices-06` | Клірик Blessed Strikes (7) і Improved Blessed Strikes (14) не пропонують вибору Divine Strike / Potent Spellcasting |
| P1 | 2024 | data | `L05-class-choices-07` | Варвар Primal Knowledge (3) не дає обрати додаткову навичку |
| P1 | 2024 | missing-system | `L05-class-choices-08` | Жоден із 48 підкласів 2024 не має жодного вибору: subclass_choice_option під RULES_2024 порожня |
| P1 | 2024 | data | `L05-class-choices-09` | Паладин 2024 без опції Blessed Warrior, Слідопит 2024 без Druidic Warrior у бойовому стилі |
| P1 | 2024 | data | `L05-class-choices-10` | Виклик Thirsting Blade відсутній серед 31 виклику 2024, хоча Devouring Blade у тих самих даних вимагає його як передумову |
| P1 | 2024 | missing-system | `L06-subclasses-05` | У підкласів 2024 немає жодного структурованого вибору — таблиця subclass_choice_option порожня, тож Circle of the Land, Hunter's Prey, Elemental Affinity і маневри Battle Master не вибираються |
| P1 | 2024 | data | `L06-subclasses-06` | ARTIFICER_2024 пропонується у конструкторі 2024, але має 0 підкласів при subclass_level = 3 — на 3-му рівні майстер показує порожній крок «Підклас» і блокує «Далі» |
| P1 | 2024 | data | `L08-levelup-machine-03` | Метамагія чародія 2024 не існує як вибір: жодного class_choice_option для SORCERER_2024 |
| P1 | 2024 | data | `L08-levelup-machine-04` | У редакції 2024 немає жодного вибору всередині підкласу — крок subclass-choices не зʼявиться ніколи |
| P1 | 2024 | bug | `L09-sheet-derived-06` | Монах 2024: беззбройного удару не існує як зброї, а Спритні атаки не діють — посох рахується від Сили |
| P1 | 2024 | data | `L09-sheet-derived-07` | Драконяча живучість 2024 не додає хітів — половина риси (КЗ) працює, половина (хіти) ні |
| P1 | 2024 | data | `L09-sheet-derived-12` | Бойові стилі 2024 не мають механіки: Оборона не дає +1 КЗ, Дуель +2 шкоди, Стрільба +2 атаки |
| P1 | 2024 | bug | `P1-human-fighter-04` | Обрані на кроці конструктора 3 види майстерності зброї не зберігаються — на листі «Обрано 0 з 3» |
| P1 | 2024 | data | `P1-human-fighter-05` | Жоден із 13 класів 2024 не має навичок класу — class.skill_proficiencies = null, конструктор вибору не пропонує |
| P1 | 2024 | data | `P1-human-fighter-06` | Риса Людини «Вправність» (Skillful) не дає володіння навичкою — вибору немає ніде |
| P1 | 2024 | bug | `P1-human-fighter-07` | Три навички, обрані в рисі «Умілець» (Skilled), не зберігаються — ні pers_skill, ні _ChoiceOptionToPers |
| P1 | 2024 | data | `P2-elf-wizard-06` | Жоден із 13 класів 2024 не має стартових навичок на вибір — class.skill_proficiencies = null, тож Чарівник 2024 недоотримує 2 навички |
| P1 | 2024 | bug | `P2-elf-wizard-07` | Риса походження 2024 (Magic Initiate у Мудреця) чіпляється без жодного вибору — кроку «Опції риси походження» не існує для всіх 16 походжень 2024 |
| P1 | 2024 | data | `P3-multiclass-wizard-cleric-03` | У всіх 13 класів RULES_2024 skill_proficiencies = NULL — крок «Навички» не пропонує жодного класового вибору |
| P1 | 2024 | bug | `P3-multiclass-wizard-cleric-04` | Друга «Посвячений у магію» від Універсальності Людини не збереглася, а вибори списків заклинань не збереглися жодного разу |
| P1 | 2024 | data | `P5-druid-secondary-flows-07` | Первісне призначення (Primal Order) друїда 2024 не має вибору Warden / Magician — жодного class_choice_option у класі |
| P1 | 2024 | data | `P6-class-sweep-level1-02` | Жоден із 13 класів 2024 не дає володінь навичками — крок «Навички» не пропонує класового вибору |
| P1 | 2024 | data | `P6-class-sweep-level1-03` | Жоден клас 2024 не дає володінь інструментами (Herbalism Kit, Thieves' Tools, 3 інструменти барда, вибір монаха) |
| P1 | 2024 | missing-system | `P6-class-sweep-level1-05` | Божественний орден (Клірик) і Первісний орден (Друїд) не пропонують вибору і не дають нічого |
| P1 | 2024 | data | `P6-class-sweep-level1-06` | Пройдисвіт 2024 не отримує Експертизи на 1-му рівні — крок «Експертиза» не зʼявляється в жодного класу |
| P1 | 2024 | data | `P6-class-sweep-level1-09` | Риса Людини 2024 «Skillful» не дає навички за вибором |
| P2 | 2024 | missing-system | `L02-backgrounds-03` | Зброя й речі зі стартового пакунка походження лягають вільним текстом, а не в інвентар — списом і кинджалами не можна битися |
| P2 | 2024 | missing-system | `L02-backgrounds-04` | Конкретний ігровий набір / музичний інструмент / реміснича спеціальність ніколи не обираються — і у володіннях, і в майні лишається «(на вибір)» |
| P2 | both | missing-system | `L02-backgrounds-05` | Походження й будь-який його вибір не можна змінити після створення — тільки перестворити персонажа |
| P2 | 2024 | data | `L03-feats-09` | Skilled: пропонує лише 18 навичок (книга дозволяє й інструменти), а опис риси втратив сам бенефіт |
| P2 | 2024 | data | `L05-class-choices-11` | Передумови викликів 2024 перенесені частково: сім викликів «Level 2+» мають порожні prerequisites, а вимога Thirsting Blade у Devouring Blade втрачена |
| P2 | 2024 | missing-system | `L05-class-choices-12` | Правило 2024 «на кожному рівні можна замінити один вибір» (виклик, метамагія, бойовий стиль, замовляння) не реалізовано — крок «Заміни» для персонажа 2024 не з'явиться ніколи |
| P2 | 2024 | data | `P1-human-fighter-11` | Риса «Умілець» пропонує лише 18 навичок — інструментів, дозволених книгою, у списку немає |
| P2 | both | bug | `P7-mobile-ux-03` | У кроці «Опції класу» назва потойбічного виклику не показана взагалі: заголовок картки і її опис — той самий короткий опис |
| P3 | 2024 | data | `P1-human-fighter-16` | Володіння Солдата «оберіть один ігровий набір» не обирається — записано узагальнене «Ігровий набір» |

## Журнал

### 2026-09-05 — початкова перевірка й межа імпорту підкласів

До змін коду `bun run test:integration tests/rules-2024/multiclass-fifteen.test.ts`:
**27 passed (27)**, 208,08 с. Тунель перевірений, прогін через штатний замок `spells_test`.
MD5 golden-JSON: `5b39d249eb9775d4a3c8454c9e35225c`; фікстури не перегенеровані.

**Рішення власника в чаті:** Артифайсера не ховати; додати підкласи, джерело класу —
Eberron: Forge of the Artificer. Власник окремо надав шість сторінок Wikidot
(Alchemist, Armorer, Artillerist, Battle Smith, Cartographer, Reanimator) та підтвердив,
що для цих матеріалів слід брати Wikidot. Reanimator у скачаній сторінці має джерело
Ravenloft: The Horrors Within, тому при імпорті не можна приписувати йому Eberron.

**Відтворена причина втрати:** у `data/2024/source/raw/subclass/` є всі 68 сторінок старого
знімка, але `normalized/subclasses.json` містить лише 48 PHB-підкласів. Шість сторінок
Артифайсера не загубив скрейпер: їх відсік обсяг нормалізації PHB, тоді як сам клас додали
як виняток. Це прямо записано в журналі KR6.2 від 2026-08-15. Ще 14 скачаних підкласів
інших класів відсіяні так само. Звірка актуальних 13 сторінок класів Wikidot знайшла 76
посилань на підкласи — ще вісім відсутні й у старому HTML. Подробиці та межі доказу —
у [звіті звірки](subclass-import-audit-2026-09-05.md).

### 2026-09-05 — опції походження й стартові навички

`MultiStepForm` тепер використовує `getOriginFeatRequirement` наявної стратегії редакції,
коли `backgroundFeatId` не заданий. Тому фіксована риса походження 2024 з опціями
вмикає крок `backgroundFeatChoices`, а риса без опцій не додає порожнього кроку.
Шлях явно обраної риси 2014 збережений. Завантажувач і збереження на сервері вже мали
`backgroundFeatId ?? originFeatId`; нової серверної логіки цей диф не додає.

`normalized/classes.json` дістав `skillProficiencies` для всіх 13 класів, а
`classSeed2024` передає його в upsert. Кількість і список узяті з Core Traits у SRD,
для Артифайсера — зі збереженого Wikidot HTML. Сід `--only classes` застосований **лише
до spells_test**, під замком: 13 класів, 187 класових рис, 0 помилок, 15,1 с.

**Докази тестів:**

- `tests/components/background-origin-step.test.tsx`: до правки 1 failed / 3 passed;
  після — 4 passed. Контрольна інверсія умови `hasBackgroundFeatChoices` дала 4 failed;
  умову відновлено. Перше технічне падіння на localStorage у Node не зараховувалось
  як доказ: тестова persistence замінена на сховище без запису на диск.
- `tests/content/class-skills-2024.test.ts`: до заповнення джерела — 1 failed, після —
  1 passed. Гейт читає таблиці книги, не числа з пам’яті.
- `tests/db/class-skills-2024-seeded.test.ts`: до сіду — 1 failed, після — 1 passed.
  Файл входить в інтеграційний набір через `tests/db/**`, у набір без бази не потрапляє.
- Контрольна зміна кількості навичок варвара 2 → 3 у source JSON зробила червоними
  обидві звірки: «книга → файл» і «файл → база», по 1 failed. Джерело відновлено;
  помилкове число у базу не сіялось.

`bunx tsc --noEmit --incremental false` і ESLint по п’яти змінених кодових/тестових
файлах — 0 помилок, 0 попереджень. `clean-code-guard: clean`.

**Межа цього виміру.** Повний гейт усіх виборів 1–20 рівнів ще не створений:
новий гейт перевіряє тільки стартові навички. Інструменти, експертиза, ордени,
метамагія, інші класові й підкласові вибори лишаються невиконаними; поява кроку
походження не закриває окремі дефекти списку Magic Initiate чи інструментів Skilled.
На момент цього першого виміру розширення імпорту ще узгоджувалося.
Подальший стан підкласів описано в наступному записі журналу. Жоден широкий критерій «Готово, коли»
не позначений виконаним і жодна з 48 знахідок не оголошена повністю закритою.

**Викочування навичок потребує власника:** `bun run seed:2024:prod --only classes`,
потім `bun run generate:creator-content` та `bun run generate:classes`, далі звичайний
деплой. У цьому проході production не змінювався. Спільні генеровані каталоги не
перезаписувалися тестовими ID; наскрізний браузерний прохід по оновленому каталогу
ще не виконаний.


### 2026-09-05 — інвентар Wikidot і шість підкласів Винахідника

Після уточнення джерел власник кілька разів підтвердив продовження. Поточний обсяг
доімпорту — усі 28 виявлених пропусків, зі збереженням їхніх книг. **Завершено контент
лише шести підкласів Винахідника; решта 22 ще не нормалізовані.**

Збережено актуальні 28 HTML підкласів та окремі 13 HTML сторінок класів.
`class-subclass-index.json` і `subclass-inventory.json` містять URL, дату та SHA-256.
Парсер `scripts/2024/parse-subclasses.ts` читає всі 76 сторінок без фільтра книги;
результат — `source/subclasses-extracted.json`. Виправлено два емпірично виявлені
дефекти парсера: пропуск lowercase `level 10` у Diviner і втрату крапки в назві
`Oath of Glory Spells.`. Структура вже наявних PHB-рис збережена.

До `normalized/subclasses.json` додано Alchemist, Armorer, Artillerist, Battle Smith,
Cartographer та Reanimator: **54 підкласи загалом, 40 нових рис**. Повні українські
описи й таблиці перекладені вручну з Wikidot. Google Translate використано лише
для окремої проби термінів; його результат до каталогу не включено. Назви заклинань
узяті зі словника, посилання ведуть на `/2024/spells/…`. Перші пʼять підкласів мають
`source: EFA`, Reanimator — `RHW`; кожен новий запис має `sourceUrl`.

У `db/changes/2026-09-05-kr31.2-subclasses-2024.sql` підготовлено 17 enum-значень для
всього обсягу доімпорту. SQL застосований **тільки до spells_test**. Схему отримано
через `prisma db pull` з тестової бази після перевірки diff: лише 17 enum-значень;
клієнт перегенерований. Схема руками не редагувалася, production DDL не виконувався.

Сід `--only subclasses` у spells_test: **54 upserted, 281 features, 0 errors, 36,8 с**.
Інтеграційна звірка `artificer-subclasses-2024-seeded.test.ts` до сіду бачила порожній
масив замість шести підкласів; після сіду — 1 passed. Після наступного додавання
посилань на заклинання потрібен повторний сід, щоб база знову дослівно збігалася.

Генератор класів переносить книгу підкласу з нормалізованого джерела; картка підкласу
показує її власну книгу. Для 2014 непідтверджену книгу не вигадуємо. Нові назви є
в обох мапах перекладів, включно з `_2024`, дзеркало словника синхронізоване.

**Докази:**

- `subclass-source-2024.test.ts`: 5 passed; звірка всіх посилань із 13 сторінок класів,
  SHA-256, структура рис і переклад шести підкласів. Видалення першого запису інвентарю
  зробило тест червоним; інвентар відновлено. Тест імпорту Винахідника до додавання
  даних падав на відсутньому Alchemist.
- `subclass-catalog-source.test.ts`: Reanimator → RHW, Alchemist → EFA, Champion →
  PHB_2024; невідомий підклас 2024 зупиняє генерацію. Контрольна підміна всіх книг
  на EFA дала червоний тест; код відновлено.
- Цільові тести імпорту, посилань, інвентарю, атрибуції та дзеркала: **72 passed**.
- ESLint по восьми кодових/тестових файлах — чистий. `clean-code-guard: clean`.
- Поточний TypeScript має помилки відсутніх `bastion*Translations` у спільному
  `translation.ts`; ці експортні мапи в цьому проході не видалялися.
- Повний `test:no-db` після імпорту був червоним. Власні помилки словникового
  походження назв, `_2024` мап і покриття spell links виправлені й перевірені.
  **`subclass-spellcasting-2024` лишається червоним: source 54, production-каталог 48.**
  Тест не послаблено. Інші падіння поточного дерева включають відсутні матеріали
  бестіарію, старі терміни й апострофи у спільних файлах, radiant-term-sweep.

**Незавершено:** інші 22 підкласи, структуровані опції всіх класів/підкласів,
механічні гранти нових підкласів (сам переклад їх не реалізує), каталог конструктора,
браузерний прохід і фінальна регресія. KR і жоден широкий критерій не закрито.
Production rollout після завершення роботи: власник застосовує SQL, виконує сід
потрібних модулів, генерує каталоги з production ID та деплоїть. Тестові ID у спільні
генеровані каталоги не записувалися.

### 2026-09-05 — метамагія, точні інструменти й 57 підкласів

Метамагія 2024 нормалізована як 10 опцій із ціною та повним українським описом.
`seedMetamagic2024` звʼязує їх із Чародієм на рівнях 2/10/17; правило вимагає рівно
по дві нові різні опції. До сіду DB-тест бачив 0 опцій, після — 10. Контрольні
мутації ціни та кількості виборів зробили відповідні тести червоними; дані відновлені.

`class-tools.json` тепер є джерелом точних інструментів класів: Бард обирає 3 з 10
музичних інструментів, Монах — 1 з 17 ремісничих або 10 музичних, Друїд фіксовано
володіє набором травника, Пройдисвіт — інструментами злодія, Винахідник — інструментами
злодія й лудильника та обирає ще один із решти 16 ремісничих наборів. Додано 22
конкретні значення `ToolCategory` через
`db/changes/2026-09-05-kr31.2-tool-categories-2024.sql`; SQL застосовано лише до
`spells_test`, а `schema.prisma` отримано через `db pull`, не ручною правкою.

Створення персонажа 2024 тепер серверно перевіряє належність, точну кількість і
унікальність class choices першого рівня. Гілка 2014 явно пропускає новий гейт.
Контрольна мутація Барда 3 → 2 дала очікуване падіння й була відновлена. Clean-code
перегляд додатково закрив спробу винести четвертий валідний ID у вигадану групу та
порожні масиви від форми. Наскрізні тести довели збереження трьох інструментів Барда,
фіксованих і вибраного інструмента Винахідника та перший рівень Барда в мультикласі.
Базові середні обладунки Друїда прибрано: вони мають прийти лише від ще не реалізованого
вибору Primal Order → Warden.

Після повторного idempotence-сіду в `spells_test`: 53 class-tool links без дублів.
Повʼязані unit/content тести — 15 passed; DB-регресія класів, метамагії та інструментів —
17 passed; `acceptance-ten` — 29 passed / 1 todo; `multiclass-fifteen` після додавання
обовʼязкових інструментів і метамагії у fixture — 27 passed. TypeScript не показав
помилок змінених файлів; загальний `tsc` зупиняється на трьох stale `.next` імпортах
відсутніх API routes.

До нормалізованого корпусу також додано Arcana Domain, Knowledge Domain і Grave Domain
з повним українським текстом та власними книгами/URL. Поточний стан: **57 із 76**
підкласів, **296** засіяних subclass features, 0 seed-помилок. Новий загальний DB-гейт
звіряє кожен normalized record і кожну його фічу зі `spells_test`; 57/57 зелені.
Залишилося нормалізувати 19 уже успішно scraped non-PHB підкласів. Отже причина втрати
доведена: scraper має всі 76 сторінок, PHB-only етап нормалізації відкинув 20; три з них
уже повернуто.

Production не змінювався, shared generated catalog не перезаписувався тестовими ID.
Після завершення всього KR власник має застосувати обидва DDL
`2026-09-05-kr31.2-{subclasses,tool-categories}-2024.sql`, виконати
`seed:2024:prod --only classes,class-tools,subclasses,metamagic`, потім згенерувати й
закомітити production-каталоги. До цього `subclass-spellcasting-2024` правильно лишається
червоним: production-derived catalog має 48 записів, normalized source вже 57.

### 2026-09-06 — 76 підкласів, структуровані вибори, гейт проти книги й регресія

Продовження перерваного проходу. Корпус доведено до **76/76** нормалізованих підкласів
(усі, що дає скрейп 13 сторінок класів), засіяно в `spells_test`: 76 підкласів, 400 фіч,
0 помилок. Причина втрати підтверджена остаточно: скрейпер мав усі 76 сторінок, PHB-only
етап нормалізації відкидав 28.

**Що тепер обирається.** Джерелом усіх виборів стали перевіряні артефакти, а не код сіду:
`data/2024/normalized/class-choices.json` (6 груп: Божественний орден, Первісний орден,
Благословенні удари, Стихійна лють, Blessed Warrior паладина, Druidic Warrior слідопита) і
`subclass-choices.json` (5 груп, 33 опції: Коло землі, Здобич мисливця, Захисна тактика,
Стихійна спорідненість, 20 маневрів Майстра бою). Метамагія, класові інструменти й виклики
лежать у власних файлах, заведених раніше цим самим KR. Кожна група має `featureEng` —
англійську назву риси, якою її знаходить гейт у книзі.

Експертиза заведена метаданими на п'яти рисах (`Bard 2` + повтор на 9, `Rogue 1` + повтор на 6,
`Ranger 2` Deft Explorer, `Ranger 9`, `Wizard 2` Scholar); сід створює окремий рядок на кожен
повтор, бо схема тримає одну прив'язку клас↔риса. Первісне знання варвара дає навичку на вибір
із шести. Thirsting Blade доданий 32-м викликом — його вимагає Devouring Blade у тих самих даних.
Базові середні обладунки друїда прибрано: тепер вони приходять лише від Primal Order → Warden,
як у книзі.

**Гейт п.7 читає книгу, а не репозиторій.** Наявний `class-subclass-choices-2024` звіряв артефакт
із runtime-правилом — обидва боки наші, тож жодна спільна помилка ним не ловилась. Доданий
`tests/content/class-choice-levels-2024.test.ts` бере рівні й кількості з
`data/2024/srd/classes.md` (заголовки «#### Level N: Назва» в секції класу) і зі збереженої
сторінки Wikidot для Майстра бою, якого в SRD немає. Чотири контрольні зламú зробили його
червоним і були відновлені: Благословенні удари 7 → 8, маневри 3 → 2, експертиза барда 2 → 3,
другий грант барда 9 → 10.

Гейт одразу знайшов справжню ваду в собі: механіка риси лежить то в англійському рядку
(`skillProficiencies` варвара), то в перекладеному (`skillExpertises` усіх п'яти) — сід читає
обидва, а перша версія гейта лише англійський, тобто мовчки не покривала жодного надання
експертизи. Виправлено читанням обох боків; окрема перевірка падає, якщо джерело втратить
хоч одне з п'яти надань.

**Регресія 2014 знайдена й прибрана.** Гейт експертизи стояв у створенні й підвищенні рівня
**без перевірки редакції** — і бив по персонажах 2014, для яких експертиза приходить іншим
шляхом (`expertiseSchema` вільним полем). `tests/golden/creation.test.ts` падав на семи
фікстурах; після додавання умови `RULES_2024` в обох місцях — на чотирьох, і всі чотири вже
не наші (див. нижче). Це і є доказ виправлення: почервоніли рівно ті три, що скаржилися на
експертизу (`bard-full-caster`, `prodigy-feat`, `longest-path`), і рівно вони позеленіли.

**Чужі зміни, які не наші й не лагодяться тут.** Паралельна сесія KR31.12 навмисно прибрала з
2014-чарівника порожній дублікат риси «Spellcasting» (`prisma/seed/classFeatureLinks2014.ts`,
`FORBIDDEN_CLASS_FEATURE_LINKS_2014`) — через це чотири golden-фікстури створення показують
`- "Spellcasting"`. Інша зміна в `src/server/db/rest-actions.ts` (BUG-010) навчила довгий
відпочинок рахувати слоти за рівнем заклинача, а не персонажа — через це
`golden/derived-state/pooled-resources` показує 0 слотів там, де фікстура пінувала 4/2 для
пройдисвіта-Соулнайфа 3-го рівня (у якого їх і не має бути). Обидві golden-фікстури
перегенеровує та сесія за рішенням власника; ми їх не чіпали, md5 дерева golden не змінився.

**Фікстури приймання довелося довчити книзі.** Обов'язковий вибір — це зміна контракту
створення й підвищення: клірик без Божественного ордену, пройдисвіт без експертизи й
Майстер бою без трьох маневрів більше не створюються. Тому шість фікстур десятки й одинадцять
фікстур п'ятнадцятки дістали ті вибори, яких вимагає книга, а харнеси — три нові поля:
`subclassChoices`, `skillChoices` (навички від риси) і `classSkills` (чотири класові навички
пройдисвіта, без яких у барда-мультикласника просто немає чого брати в компетентність).
Порівняння опції з базою тепер терпить назву без суфікса редакції, щоб фікстура писала
`Divine Order: Protector`, а не `Divine Order: Protector (2024)`.

Тести, що міряють щось інше (спорядження походження, редакція персонажа, класові інструменти),
беруть обов'язкові вибори через новий спільний хелпер `findRequiredClassChoices2024` — він питає
базу й правило пулу, а не знає напам'ять, що воїн мусить обрати бойовий стиль.

**Дві звірки зрізу бази виправлені разом із доказом, а не підігнані.** `features-2024-seeded` і
`ruleset-2024-isolation` тримали `48` підкласів числом; тепер обидві читають довжину
`data/2024/normalized/subclasses.json`, а перелік рівнів надання фіч виводиться з того ж джерела.
Зафіксоване число робило б їх доказом старого стану, а не сіду.

**Доімпорт підкласів доведено до вимог власних гейтів.** Дев'ятнадцять записів, доданих
попереднім проходом, не мали ні підзаголовка, ні вступного тексту — ані англійського, ані
українського, — і через це `rules2024-import` був червоний на двох критеріях. Англійські
вступи витягнуто зі збережених сторінок Wikidot (рядок після «Source:» — підзаголовок,
наступний — вступ), українські перекладено вручну за скілом перекладу: власні назви поза
ратифікованими реєстрами несуть маркер оригіналу (`Місячних Островів{{Moonshae Isles}}`,
`чарополумʼя{{spellfire}}`, `Долини Крижаного Вітру{{Icewind Dale}}`), а вже усталені форми
корпусу — ні (`Тіньового Долу`, `Стихійних Планів`, `Фаеруні`).

**Рішення власника в чаті (2026-09-06):** 14 коінованих назв підкласів ратифіковано в
`dictionary.json` (`rules2024.newSubclassNames`) **з великих літер** — як уже записані
«Колегія Танцю» й «Шлях Дикого Серця». Вісім назв, що стояли з малої, приведено до цієї форми
в `translation.ts`, у нормалізованому корпусі й у дзеркалі словника.

**Виявлено при регенерації: продакшн уже містить усі 76 підкласів 2024.** `generate:creator-content`
жорстко ходить `--target prod`, і згенерований із проду каталог показав і Порожнистого Вартового,
і Колегію духів, і Покровителя-Релікта. Прямий read-only запит підтвердив: `select count(*) from
subclass where ruleset='RULES_2024'` у базі `spells` → **76**. Тобто попередній запис журналу
«Production не змінювався» щодо підкласів уже неправдивий — сід підкласів у прод виконано.
ID проду й клона розійшлися (`HOLLOW_WARDEN` — 577 проти 846), а рядків самих виборів
(`subclass_choice_option` під `RULES_2024`) у проді **нуль**.

**Чому це важить для тестів.** Після KR22.5 майстер підвищення бере класи, підкласи та їхні
вибори не з бази, а з `src/lib/generated/creator-content-2024.json`
(`loadLevelUpBaseContent` → `findCharacterCreatorOptions`). Каталог, згенерований із проду, не
містить жодного підкласового вибору — і приймальні матриці падали з «Обрана опція недоступна на
цьому рівні», хоча сама логіка правильна. Каталог, згенерований із клона, дає зелену сітку.

**Рішення: генерувати з `spells_test`,** бо `prebuild` (`npm run generate:content`) все одно
перезнімає **всі** каталоги з проду перед кожною збіркою — закомічений файл є тимчасовим
артефактом, а не тим, що їде на сервер. Тестові ID у git при цьому потрапляють, і це свідомий
компроміс: інакше сітка з 27 + 30 приймальних критеріїв міряла б не правила, а стан викочування.

Заразом у клон долито `--only races,feats`: його текст видів і рис відставав від проду, і саме
через це впала звірка покриття посилань на заклинання (`creator-content-2024.json` 450/500
проти планки 353/390). Після доливання — 455/500, гейт зелений. Це стан клона, не наш текст:
у `data/2024/normalized/subclasses.json` штатний `link-spell-mentions` показує 369 із 376.

**Єдиний гейт, що лишається червоним:** `radiant-term-sweep` — «променист-» у генерованих
каталогах (`classes.json` 18, `creator-content-2024.json` 10, `creator-content-2014.json` 5 —
останні пʼять лежать так уже в HEAD). Термін зачищає паралельна ціль O32 у тексті бази; поки
її прохід не пройдено, будь-яка регенерація повертає слово в каталоги. Це не наша регресія й
не привід правити тест — той самий випадок, що описаний у CLAUDE.md для апострофа.

**Що має зробити власник для викочування:** `bun run seed:2024:prod --only subclasses` (переллє
виправлені назви, підзаголовки, вступи й парний маркер), потім `bun run generate:creator-content`
і `bun run generate:classes`, далі звичайний деплой. Класові й підкласові вибори, метамагія,
інструменти та експертиза потребують ще `--only classes,class-tools,class-choices,subclass-choices,metamagic,fighting-styles,invocations`
і обох DDL із `db/changes/2026-09-05-kr31.2-*.sql`, якщо їх ще не застосовано до проду.

**MD5 golden-дерева зрушив не цим проходом.** На старті було
`5b39d249eb9775d4a3c8454c9e35225c`, наприкінці — `0e53a4eb720fa490ffaf01ecc36939cf`. Змінені
рівно шість фікстур (`creation/{wizard-no-subclass,variant-human-skill-expert,elf-high-subrace,
fighting-initiate-feat}.json`, `derived-state/{pooled-resources,rest-and-slots}.json`), і їхній
диф — це прибрана порожня «Spellcasting» та перерахунок слотів після відпочинку, тобто
[KR31.12](kr31.12-2014-regressions.md), закритий тією самою датою паралельно. Час зміни файлів
(14:11–14:12) збігається з її прогоном; цей прохід `test:golden:update` не запускав і жодного
файла в `tests/golden/` не редагував.

**Що з 48 знахідок лишається червоним.** Закрито 32 (29 цим KR, три — сусідньою роботою над
видами й рисами). Незакриті шістнадцять і причина, чому вони не тут:

- походження: `L02-backgrounds-02` (список заклинань Посвяченого у магію), `-03` (пакунок
  походження лягає текстом, а не в інвентар), `-04` (конкретний ігровий набір / інструмент не
  обирається), `-05` (походження не змінити після створення), `P1-human-fighter-16`;
- риси: `P1-human-fighter-06` і `P6-class-sweep-level1-09` (людська «Вправність»),
  `P1-human-fighter-11` (Умілець не пропонує інструментів), `P3-multiclass-wizard-cleric-04`
  (друга «Посвячений у магію» й вибори списків не зберігаються);
- лист і похідні: `L09-sheet-derived-06` (беззбройний удар монаха), `-07` (Драконяча живучість
  не додає хітів), `-12` (бойові стилі 2024 без механіки);
- решта класових виборів: `L05-class-choices-11` (сім викликів «Level 2+» із порожніми
  передумовами — Thirsting Blade закрито, решта ні), `-12` (правило заміни одного вибору на
  рівні), `P1-human-fighter-04` (майстерність зброї не зберігається), `P7-mobile-ux-03`
  (назва виклику не показана в кроці «Опції класу»).

Підкласові вибори закриті для чотирьох підкласів, названих цим KR. Аудит усіх 76 сторінок
показав, що власні builder-вибори мають ще щонайменше Armorer, Arcane Archer, Rune Knight і
Four Elements — це наступний контентний хвіст, а не зроблене мовчки.

**Виміряні прогони 2026-09-06 (після всіх правок).**

| Прогін | Результат |
|---|---|
| `bun run test:integration` (увесь набір, 67 файлів) | 405 passed / 1 todo; чотири падіння того прогону — чужий незавершений запис (нижче) |
| `multiclass-fifteen` + `acceptance-ten` + три DB-гейти виборів | **80 passed / 1 todo**, 332 с |
| `bun run test:no-db` (те, що ганяє CI) | **2280 passed**, 2 failed — обидва `radiant-term-sweep` |
| `bunx tsc --noEmit` | 0 помилок поза трьома застарілими імпортами в `.next/` |
| ESLint по 14 змінених файлах | чисто |
| MD5 golden | `0e53a4eb720fa490ffaf01ecc36939cf` — зрушений KR31.12, не цим проходом |

До правок той самий інтеграційний набір давав **60 failed / 347 passed**, а `test:no-db` —
**7 failed / 2273 passed**.

**Чотири падіння повного прогону — чужий запис на півдорозі.** Усі чотири мали текст
`TypeError: findUsesAfterShortRest is not a function` у `src/server/db/rest-actions.ts`:
паралельна сесія зберегла виклик о 14:48, а саму функцію в `src/rules/resource-pools.ts` —
о 14:49, і прогін (старт 14:45) застав проміжок. Повторний прогін тих самих трьох файлів —
**10 passed**. Наш диф `rest-actions.ts` і `resource-pools.ts` не чіпає.

**Зламú, якими доведено нові гейти цього проходу:**

- `class-choice-levels-2024`: Благословенні удари 7 → 8; маневри Майстра бою 3 → 2;
  експертиза барда 2 → 3; другий грант барда 9 → 10; прибране надання Науковця чарівника
  (перевірка «джерело втратило надання експертизи»). Усі пʼять дали червоний, джерело
  відновлено, гейт знову зелений.
- Гейт спершу був **порожнім наполовину** й це видно з історії: він читав механіку лише з
  англійського рядка риси, а `skillExpertises` лежить у перекладеному, тож жодного з пʼяти
  надань експертизи він не покривав. Виправлено читанням обох боків, як це робить сід.

## 2026-09-07 — «Обрана опція недоступна на цьому рівні»: id каталогу проти id клона

Записано сюди, бо шлях, який відхиляв вибір, — це підкласові вибори 2024, наповнені саме цим KR.

**Симптом.** `levelUpCharacter` віддавав «Обрана опція недоступна на цьому рівні» на будь-якому
`subclassChoices` того рівня, що стоїть у `levels_granted`. Два незалежні відтворення:
`08-orc-ranger-guide` (Здобич мисливця, 3-й рівень) в `acceptance-ten` і `BATTLE_MASTER`
(Маневри майстра бою, 3-й рівень) в `repeatable-feats-2024`.

**Причина — не код і не дані, а два різні простори id.** Контент майстра підвищення їде з файлу
([Р25](../DECISIONS.md#р25)): `loadLevelUpBaseContent` бере класи з
`findCharacterCreatorOptions(ruleset)`, тобто з `src/lib/generated/creator-content-2024.json`, а
той згенеровано **з робочої бази**. Фікстура ж резолвить опцію запитом до `spells_test`. Виміряно
2026-09-07 обома базами:

| Опція | Робоча база / каталог | `spells_test` до перезняття |
|---|---:|---:|
| `Hunter's Prey: Colossus Slayer (2024)` | 3700 | 3984 |
| `Hunter's Prey: Horde Breaker (2024)` | 3701 | 3985 |
| усі 33 підкласові опції 2024 | 3696–3728 | 3984–4016 |
| `class_choice_option` 2024, сума id | 484 705 | 498 581 |
| `class_choice_option` 2014, сума id | 3 711 | 3 711 |

2014 збігається один в один, бо приїхав клоном; 2024 розійшовся, бо його сіяли **окремо** в
кожну базу, і `option_id` — автоінкремент. Сервер звіряв вибір із каталогом (id проду), фікстура
подавала id клона — жоден не потрапляв у `allowedOptionsAtLevel`, і валідація чесно казала «немає
такої опції на цьому рівні». Шлях уперше став досяжним 2026-09-06, коли цей KR наповнив таблицю:
доти `allowedOptionsAtLevel` була порожня, валідація поверталася `null`, а вибори мовчки
пропускалися.

**Рівень — P2, не P0.** Гравця це не торкалося: у проді каталог і база — одні й ті самі id, тож
майстер пропонує й приймає той самий рядок. Персонажів `RULES_2024` у робочій базі **0** (запит
2026-09-07), гейт 2024 закритий ([Р24](../DECISIONS.md#р24)). Ціна помилки все одно висока: інваріант
«id каталогу існують у базі, до якої йде запис» ніде не перевірявся, а порушення проявляється не
падінням, а відмовою з правдоподібним текстом.

**Що зроблено.**

1. `tests/db/creator-catalog-ids-resolve.test.ts` — сторож інваріанта: кожен `choiceOptionId`,
   на який посилається каталог (класові, підкласові й рисові вибори обох редакцій), існує в базі
   під тим самим `optionNameEng`. Порівняння — чиста функція
   [`creator-catalog-choice-references.ts`](../../src/lib/content/creator-catalog-choice-references.ts).
   Доведено червоним двічі: підміна id (`3700 → 999700`) і підміна назви під тим самим id — обидві
   дали адресну відмову з назвою підкласу, після відновлення каталогу зелено.
2. Каталоги перегенеровано з робочої бази (дозвіл власника 2026-09-07): `generate:feats`,
   `generate:classes`, `generate:creator-content`. `feats.json` і `classes.json` — байт у байт ті
   самі; `creator-content-2014/2024.json` виросли. Апострофний і променевий гейти **не зрушили**:
   знятих форм у creator-content було 22 і 12 до й рівно стільки ж після.
3. `spells_test` перезнято з проду (`db-clone.sh spells_test content`) і накладено сіди, яких у
   проді немає, — саме вони й були різницею.

**Чого коштувало перезняття — прод відстає від клона на п'ять сідів.** Клон повернув базу до
стану проду, і кожен тест, що спирався на сід, прогнаний лише в тесті, почервонів. Це не
регресія тестів, а вимір: перелічене нижче **не прогнано в робочій базі**.

| Сід | Що впало без нього | Наслідок у проді |
|---|---|---|
| `seed:species-levels-2024` | К11, К17 матриці десятки | риса виду видається на 1-му, а не на своєму рівні; рівневих заклинань родоводу немає |
| `seed:armor-2024` | М20 матриці пʼятнадцяти | альтернативних формул базового КЗ 2024 у каталозі обладунку немає взагалі |
| `seed:race-traits` (2014) | 4 golden створення | людина, варіант людини й Custom Lineage без своїх рис |
| `seed:2024` (числа фіч, пули) | 11 тестів `tests/db/*` | лічильники класових і підкласових фіч 2024 порожні, короткий відпочинок 2024 нічого не повертає |
| `seed:class-feature-links` (2014) | 5 тестів KR31.12 | клірик 2014 несе дублікат Каналу божественності, чарівник — порожній Spellcasting |

Це вимір, а не робота цього проходу: сіди прогнані **лише в `spells_test`**. Чи лити їх у прод —
рішення власника, і воно окремо від релізу 2024 (`race-traits` і `class-feature-links` — 2014,
тобто зачіпають 9 394 живих персонажів).

**Побічно.** `tests/content/known-no-op-choice-options.json` пінив пʼять опцій Адепта стихій 2024
за id клона (4215–4219); після перезняття вони перестали існувати, і гейт червонів з обох боків.
Замінено на id робочої бази (3983–3987), причина та сама — `BUG-005`, опору шкоди в застосунку
немає.

### 2026-09-08 — прод після прогону власника: три сіди з пʼяти, і одна регресія від порядку

Власник прогнав сіди в робочу базу й перегенерував каталоги. Виміряно запитами того ж дня:

| Сід | У проді | Доказ |
|---|---|---|
| `seed:2024` | ✅ | `feature.uses_count` під 2024 — 75 рядків |
| `seed:armor-2024` | ✅ | три альтернативні формули базового КЗ у каталозі обладунку |
| `seed:species-levels-2024` | ✅ | шість рис виду мають рівень персонажа |
| `seed:race-traits` (2014) | ❌ | рис `Human: *` і `Custom Lineage: *` у базі немає; `feature` 2014 — 1281, а має бути 1291 |
| `seed:class-feature-links` (2014) | ❌ | `CLERIC_2014` досі несе `Channel Divinity` (риса паладина), `WIZARD_2014` — порожній `Spellcasting`, `Turn Undead` клірику не привʼязано |

**Регресія від порядку кроків: риса «Ability Score Improvement» 2024 повернулася в прод.**
`seed:2024` створює її щоразу, а `seed:remove-asi-feat-2024` знімає — тож перший, прогнаний
після другого, відкочує рішення KR31.4. Виміряно: `feat` під 2024 знову **75** рядків, серед них
`feat_id 3512 ABILITY_SCORE_IMPROVEMENT`. Лікується повторним прогоном
`seed:remove-asi-feat-2024:prod --apply`; **`seed:2024:prod` завжди має йти перед ним.**

Так само не прогнано `seed:magic-items:prod` — тексти партій O14 у базі не дорівнюють файлам
(`magic-items-seeded`, `magic-items-2024-seeded`).

`spells_test` перезнято з нового проду й доведено сідами, яких у проді немає. Після цього ланцюг
зелений: решта інтеграційного **395 passed**, `acceptance-ten` **29 passed / 1 todo**,
`multiclass-fifteen` **27 passed**, `tests/golden/levelup` **23 passed**, md5 golden
`a6ab512652d916349d3e84431b644602` (не зрушив), `test:no-db` **2326 passed** із двома чужими
`radiant-term-sweep`.
