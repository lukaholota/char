# KR31.9 — Серверні гейти: доступ і передумови

**Ціль:** [O31](README.md) · **Статус:** 🟡 доступ, ASI і мертві маршрути закрито 2026-09-05,
передумови рис — свідома політика (не баг); лишається лише право співвласника на видалення ·
**Залежить від:** —

## Мета

Сервер не вірить формі: доступ до персонажа, передумови рис і мультикласу, розмір пакета ASI перевіряються на сервері.

## Виміряно 2026-09-04

Два маршрути дозволяли будь-кому **без входу** читати й змінювати чужого персонажа:
`/api/character/level-up` (GET і POST) і `/api/characters/[id]/magic-items` (POST і DELETE).
Доведено на тестовій базі: анонімний POST підняв рівень чужому персонажу, `200 {"success":true}`,
рівень 1 → 2. Обидва маршрути — мертвий код без викликів із застосунку.

Решта гейтів: сервер приймає довільний пакет ASI (одне підвищення дало +2/+2/+2 замість двох
очок); передумови риси на ASI-рівні не перевіряються ні за рівнем, ні за характеристиками;
передумова володіння не перевіряється ніде (`Heavy Armor Master` доступний чарівникові); «Додати
рису» з листа пише голий рядок `pers_feat` без гейтів, виборів, ASI й навичок; передумова
мультикласу у формі `choice` не перевіряється для 2014; співвласник за посиланням може видалити
персонажа власника.

## Що зробити

1. ✅ Спільний гард `findCharacterAccessFailure` поверх наявного `canEditPers` на всіх чотирьох
   обробниках, плюс сторож на нові маршрути персонажа.
2. Вирішити з власником долю двох мертвих маршрутів: видалити чи лишити під гардом.
3. `getLevelUpInfo` перевіряє власність, а не лише наявність сесії.
4. Валідація пакета ASI і передумов рис у `src/rules/`, спільна для форми й сервера.
5. Права співвласника: редагування не дорівнює видаленню.

## Готово, коли

- [x] Обидва маршрути віддавали 401 анонімному запиту (2026-09-04, тест на шість випадків **був
  червоним**); 2026-09-05 обидва видалено за рішенням власника — мертвий код, нема чого гейтити.
- [x] `getLevelUpInfo` не віддає чужого персонажа — 2026-09-05, тест був червоним.
- [x] Сервер відхиляє неправильний пакет ASI — 2026-09-05, тест був червоним.
- [x] Рису без виконаних передумов сервер **свідомо не** відхиляє — рішення власника 2026-09-05
  підтверджує чинну політику (Р26/Р39, KR18.3): хибний блок при можливій помилці розрахунку
  передумови дорожчий за хибний дозвіл. Закрито як «не баг», а не як залишена робота.
- [ ] Видалення персонажа доступне лише власнику — поза обсягом цієї сесії.

## Знахідки аудиту (18)

Повний доказ кожної — у [findings.md](findings.md), сирий звіт автора — у [audit/](audit/).

| Рівень | Ред. | Тип | ID | Що не так |
|---|---|---|---|---|
| P0 | both | bug | `L16-ruleset-isolation-07` | POST /api/character/level-up підвищує рівень будь-якому персонажу без автентифікації й перевірки власності, а GET віддає чужого персонажа |
| P1 | both | bug | `L03-feats-15` | Менеджер рис на листі додає рису без жодного гейта, без ASI і без виборів усередині риси |
| P1 | both | bug | `L10-sheet-config-14` | Будь-яка додана на листі зброя одразу позначається як «з володінням», хоча персонаж може нею не володіти |
| P1 | 2014 | bug | `P3-multiclass-wizard-cleric-05` | Передумова мультикласу у формі `choice` не перевіряється в RULES_2014 — Воїна пропонують персонажу з СИЛ 8 і СПР 10 |
| P2 | both | bug | `L01-species-10` | Серверна дія створення не вимагає вибору в кожній групі опцій виду — персонаж може лишитись без обовʼязкової риси |
| P2 | 2024 | bug | `L02-backgrounds-11` | Сервер не звіряє редакцію походження з редакцією персонажа: 2014-походження в запиті 2024 кидає неперехоплений виняток, а без ASI-вибору тихо створює персонажа без бонусів походження |
| P2 | 2024 | data | `L03-feats-14` | Риси «Бойовий стиль» доступні будь-кому з класового ASI: у них немає передумови «Fighting Style Feature» |
| P2 | both | bug | `L08-levelup-machine-05` | Сервер приймає довільний пакет ASI: одне підвищення дало +2/+2/+2 замість двох очок |
| P2 | both | bug | `L08-levelup-machine-07` | Сервер не перевіряє передумови риси на ASI-рівні — ні рівень, ні характеристики |
| P2 | both | missing-system | `L10-sheet-config-05` | Ліміт налаштування на три магічні предмети не рахується і не показується — можна налаштуватися на будь-яку кількість |
| P2 | both | bug | `L11-persistence-identity-05` | «Додати рису» з листа пише голий рядок `pers_feat`: без гейта категорій/повторів, без виборів, без ASI, без навичок і без рядків `pers_feature` |
| P2 | both | bug | `L12-secondary-flows-11` | Співвласник за посиланням на редагування може назавжди видалити персонажа власника; знімки власника лишаються сиротами |
| P2 | 2014 | bug | `L17-known-registries-03` | BUG-008 виправлено лише наполовину: гейт мультикласу для 2014 не читає форму `choice` (тобто Воїн 2014 без вимог взагалі) і ніколи не перевіряє поточний клас |
| P2 | both | bug | `L19-parity-competitors-07` | Ліміт «не більше трьох налаштованих предметів» не перевіряється — можна налаштуватися на скільки завгодно |
| P3 | 2024 | bug | `L03-feats-13` | Передумова «володіння» (prerequisiteProficiency) ніде не перевіряється — Heavy Armor Master доступний чарівникові |
| P3 | 2024 | data | `L03-feats-18` | Boon of Spell Recall втратив передумову «Spellcasting Feature» — доступний варварові |
| P3 | both | bug | `L08-levelup-machine-13` | getLevelUpInfo рахує потребу підкласу й ASI за рівнем основного класу, а нові фічі — за рівнем персонажа |
| P3 | both | missing-system | `L19-parity-competitors-15` | Недоступні для мультикласу класи ховаються замість того, щоб показатися з причиною — хоч у рисах цей патерн уже зроблено правильно |

## Журнал

### 2026-09-05 — доступ і пакет ASI закриті; передумови рис — не баг, а чинна політика

**`getLevelUpInfo` (src/server/db/levelup-persistence.ts:74).** Перевіряла лише сесію, не
власність. Гейт — той самий `canEditPers`, що на `/api/character*`: `findUserIdByEmail` + `canEditPers(persId, userId)`,
відмова тим самим текстом `"Character not found"`, що на відсутнього персонажа (щоб не видавати
існування чужого id). `executeLevelUp` викликає `getLevelUpInfo` першим рядком і повертає її
помилку — тож `levelUpCharacter` (реальний шлях майстра, `src/lib/actions/levelup.ts`) закрився
тим самим виправленням без окремого коду. Два «сусідні» місця з того ж файлу й
`src/lib/actions/levelup.ts` — саме ці дві функції; інших читань персонажа за `persId` у жодному
з файлів нема. Доведено `tests/actions/levelup-server-gates.test.ts`: сторонній залогінений і
GET, і POST на чужого персонажа отримує помилку, рівень не змінюється; власник далі бачить і
підвищує своє.

**Пакет ASI (L08-levelup-machine-05).** Правило — `findCustomAsiPackageProblem` у
`src/rules/abilities.ts`: одна характеристика +2, або дві по +1, кожна не більше разу, сума
рівно 2. Викликається в `executeLevelUp` перед застосуванням `customAsi`.

**Побічна знахідка, якої не було в реєстрі.** Перша версія гейта на «рівень не ASI-івський»
використала `info.isASILevel` (уже готове поле з `getLevelUpInfo`) — і зламала `tests/rules-2024/multiclass-fifteen.test.ts`
(персонаж 22, Fighter4/Paladin4, застряг на 7-му рівні замість 8-го). Причина —
`L08-levelup-machine-13` («getLevelUpInfo рахує ASI за рівнем ГОЛОВНОГО класу»): `info.isASILevel`
дивиться на `pers.classId`, а не на клас, який фактично левелапиться. У мультикласового
персонажа, чий головний клас більше не росте (Fighter лишається 1-го рівня, росте тільки
Paladin), `info.isASILevel` було `false` **завжди**, незалежно від реального рівня Paladin —
і мій же гейт відмовляв у справжньому ASI Paladin. Виправлено рахунком на місці:
`isAbilityScoreIncreaseLevel(selectedClass, classLevelAfter)` — `selectedClass`/`classLevelAfter`
уже обчислені в `executeLevelUp` для класу, що левелапиться, незалежно від того, головний він чи
ні. Тест на це — окремий кейс у тому ж файлі (Воїн 1 → мультиклас Розбійник → рівень 4 Розбійника
дає ASI, хоч Воїн і не зрушив з 1-го). Обидва варіанти гейта проведені крізь червоне.

**Передумови рис на ASI-рівні (L08-levelup-machine-07, L03-feats-13) — НЕ виправлено.** Скептик
аудиту сам це підняв на L03-feats-13: сервер справді не перевіряє `prerequisiteLevel`/
`prerequisiteAbilityScore`/`prerequisiteProficiency`, але це **двічі свідомо ухвалено власником**
— [KR18.3](../o18-2024-character-parity/kr18.3-feat-sources.md) 2026-08-28 («Передумови рис
сервер не перевіряє. **Не робимо.**») і [Р39](../DECISIONS.md#р39) 2026-09-04 («передумови рис у
цьому проєкті — попередження, а не заборона… політика «правила підказують, не забороняють»
(Р26) спільна для всіх рис обох редакцій»). Реалізувати «Готово, коли» цього KR буквально
(«сервер відхиляє рису без передумов») означало б мовчки скасувати цю політику під час сесії, що
мала лише закрити гейти доступу — тому питання винесене власнику в чат, а не вирішене тут.

**Мертві маршрути (питання 4) — видалено.** Власник обрав видалення: жоден компонент застосунку
їх не викликав (перевірено grep), а POST `/api/character/level-up` ще й ніс захардкоджений
`newLevel = 2` — ознака недописаного тестового коду, не робочої фічі. Видалено обидва
`route.ts` разом із порожніми директоріями (`src/app/api/character/level-up/`,
`src/app/api/characters/[characterId]/magic-items/`); функції під ними (`getLevelUpSteps`,
`confirmLevelUp`, `confirmMulticlassLevelUp`, `attachMagicItem`, `removeOneMagicItem`) не чіпав —
перші три далі живі під мертвим другим майстером підвищення (`src/components/level-up/`,
питання 5 у [README](README.md#питання-до-власника), не вирішене цією сесією), а
`attachMagicItem`/`removeOneMagicItem` лишаються в `src/server/db/character-api.ts`, спільному
модулі з живим маршрутом заклинань. `tests/routes/character-api-auth.test.ts` схудла до одного
опису — файлового сканування «кожен `route.ts` під `/api/character*` перевіряє доступ»
(поріг `>= 3` знижено до `>= 2`, під фактичну кількість маршрутів); шість характеризаційних
тестів на самі видалені маршрути прибрано разом із кодом, який вони описували.

**Передумови рис — рішення власника: лишити політику як є.** Питання задано в чаті: чи
скасовувати Р26/Р39 для ASI-рівня. Відповідь власника: перший варіант правильний — «в нас по
правилах, але конфігурабле», і головне застереження — «якщо ще й якийсь баг і неправильно щось
обчислилося, то це погано, якщо людина не зможе взяти потрібну рису». Тобто м'яка політика (лист
попереджає, сервер не блокує) обрана свідомо саме через ризик хибного розрахунку передумови:
хибний блок дорожчий за хибний дозвіл. Пункт 3 цієї сесії закривається як «не баг, чинна
політика», а не як залишена робота.

**Golden 2014 і приймальні набори.** `tests/rules-2024/acceptance-ten.test.ts` — 29 passed / 1
todo, без змін. `tests/rules-2024/multiclass-fifteen.test.ts` — 27/27 після фікса вище (спершу
впало 4/27 через нього ж, розібрано вище). `tests/golden/creation.test.ts` не переганявся
повторно наприкінці сесії через паралельну аварію: інша сесія (`spells-holota-family-b6`)
випадково зробила `git checkout --` на `src/lib/refs/translation.ts` і стерла чужий
незакомічений блок (бастіонні мапи), що на кілька хвилин зробило неробочим увесь ланцюжок
імпортів через `bastionsData.ts` — і разом з ним усі DB-тести, що завантажують контент
персонажа. Це стороння аварія, не регресія цієї сесії; вміст відновлено з дзеркала
`dictionary.json` (синхронізованого з `translation.ts` за конвенцією CLAUDE.md) і передано
зачепленій сесії. Планую перегнати `tests/golden/creation.test.ts` і `bun run test:no-db` ще раз
після того, як інша сесія відновить файл — окремим підсумком нижче або наступною сесією, якщо
токени скінчаться раніше.
