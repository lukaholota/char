# KR31.4 — Механіка рис 2024

**Ціль:** [O31](README.md) · **Статус:** ✅ 11 з 11 знахідок закрито (2026-09-06 і 2026-09-14), прод прогнаний; поза KR — Tavern Brawler і зміна опорів після відпочинку · **Залежить від:** —

## Мета

Риса 2024 робить те, що написано в книзі, а не лише додає +1 до характеристики.

## Виміряно 2026-09-04

З 75 рис 2024 **жодна** не видає володіння обладунком, зброєю чи інструментом: `Lightly /
Moderately / Heavily Armored`, `Martial Weapon Training`, `Chef`, `Poisoner` дають лише текст. Усі
десять рис категорії «Бойовий стиль» — проза: `Defense` не дає +1 КЗ, `Dueling` не дає +2 шкоди,
`Archery` не дає +2 до атаки. `Alert` не додає бонус майстерності до ініціативи (взагалі жодна
сутність не може вплинути на ініціативу). `Speedy` і `Boon of Speed` не міняють швидкості.
`Skill Expert` не дає ні володіння, ні експертизи. `Weapon Master` не додає слота майстерності,
хоча система майстерності повністю реалізована. `Ability Score Improvement` присутня в списку рис
і не робить **нічого** — витрачене підвищення 4-го рівня. Епічні дари не мають механіки, крім +1,
а стеля характеристики зашита як 20 замість 30.

## Що зробити

1. Розширити модель надань риси до того, що книга справді дає: володіння, швидкість,
   ініціатива, ХП, опір, експертиза, ресурс.
2. Провести їх через `bonus-calculator` тим самим шляхом, яким уже їдуть расові бонуси.
3. `Ability Score Improvement` як рису прибрати або звести з механізмом ASI рівня.
4. Стеля 30 для епічних дарів (перетинається з KR27.9 — не дублювати, узгодити).

## Готово, коли

- [x] Бойовий стиль «Оборона» дає 17 КЗ у кольчузі, а не 16. Фіча
  `Fighting Style: Defense (2024)` несе `gives_ac=1` і `requires_armor_for_ac_bonus=true` — ті
  самі колонки, які `bonus-calculator` уже читав для стилів 2014. Критерій закріплений у
  `tests/rules/armor-class.test.ts`: 16 без риси, 17 з нею, 10 без обладунку.
- [x] `Alert` змінює ініціативу на листі. Нова колонка `feature.initiative_proficiency`,
  правило `findInitiativeProficiencyBonus`, і `calculateFinalInitiative` — та сама функція, яку
  малюють `MainStatsSlide` і `generateCharacterPdf`.
- [x] Риси, що дають володіння, дають їх у даних, а не в тексті — шість рис із шести, чиє
  надання називає enum, який у застосунку існує. **Три лишаються текстом:** Crafter і Musician
  просять вибрати три інструменти, а `ChoiceOptionEffectKind` має лише `ASI`,
  `SKILL_PROFICIENCY` і `SKILL_EXPERTISE`; Tavern Brawler дає володіння імпровізованою зброєю,
  якої в `WeaponType` немає. Це названа межа, а не пропуск.
- [x] Жодна риса не «витрачається намарно» — рядок `ABILITY_SCORE_IMPROVEMENT` прибраний із
  переліку рис 2024 рішенням власника.

## Знахідки аудиту (11)

Повний доказ кожної — у [findings.md](findings.md), сирий звіт автора — у [audit/](audit/).

| Рівень | Ред. | Тип | ID | Що не так |
|---|---|---|---|---|
| P1 | 2024 | data | `L03-feats-01` | Усі 10 рис категорії «Бойовий стиль» 2024 — сама проза: +2 до дальньої атаки (Archery), +1 КЗ (Defense), +2 шкоди (Dueling) не доїжджають до листа |
| P1 | 2024 | data | `L03-feats-02` | Жодна з 75 рис 2024 не видає володіння обладунком, зброєю чи інструментом — Lightly/Moderately/Heavily Armored, Martial Weapon Training, Chef, Poisoner, Crafter, Musician, Tavern Brawler порожні |
| P1 | 2024 | data | `L03-feats-03` | Skill Expert не дає ні володіння навичкою, ні експертизи — лише +1 характеристики |
| P1 | 2024 | data | `L03-feats-04` | Прирости швидкості від рис (Speedy +10 футів, Boon of Speed +30) не доїжджають до листа |
| P1 | 2024 | missing-system | `L03-feats-05` | Alert не додає бонус майстерності до ініціативи — головний числовий ефект риси відсутній |
| P1 | 2024 | data | `L03-feats-12` | Weapon Master не додає слота майстерності зброї, хоча система майстерності в проєкті повністю реалізована |
| P1 | 2024 | data | `L03-feats-16` | Риса «Ability Score Improvement» присутня в списку рис і не робить нічого — витрачене підвищення 4-го рівня |
| P1 | 2024 | data | `L03-feats-19` | Епічні дари: жодної механіки, крім +1 характеристики — Boon of Fortitude +40 HP, Boon of Skill «володіння всіма навичками», Boon of Energy Resistance (вибір 2 типів шкоди) не існують |
| P1 | 2024 | missing-system | `L09-sheet-derived-04` | Ініціатива не знає риси Пильність 2024 (+БМ) — жодна фіча чи риса не може вплинути на ініціативу взагалі |
| P2 | 2024 | data | `L03-feats-08` | Elemental Adept не має вибору типу шкоди, тому й повтор нічим не обмежений — рису можна брати нескінченно з тим самим ефектом |
| P2 | 2024 | bug | `L03-feats-17` | Епічні дари: стеля характеристики зашита як 20, хоча книга дозволяє до 30 |

## Журнал

### 2026-09-06 — прохід KR31.4

**База, знята ДО роботи** (дерево вже було брудне — 177 незакомічених файлів від кількох сесій):

| Що | Число на старті |
|---|---|
| `bun run test:no-db` | `2 failed \| 2293 passed (2295)` |
| md5 дерева golden | `a6ab512652d916349d3e84431b644602` |

Обидва падіння — у `tests/content/radiant-term-sweep.test.ts`, носії сесії O32. Чужі, не чіпав;
на кінець проходу вони ті самі два.

### Перевірка знахідок наново

Аудит зупинили на ліміті токенів, тож 7 із 11 знахідок KR31.4 не мали вердикту скептика. Кожну
переграв запитом до `spells_test` — **усі 11 підтвердилися, жодної неправдивої**. Два уточнення,
яких реєстр не називав:

- **`L03-feats-17` (стеля 30) виявився вже закритим** — не мною і не в цьому проході. KR27.9 звів
  стелю в одну чисту функцію `src/rules/ability-score-ceiling.ts` (20 звичайно, 30 для
  `EPIC_BOON`), і `grep "Math.min(20"` по `src/` більше не дає жодного місця в шляхах
  характеристик — лише обмеження рівня в таблицях слотів. Другої константи не заводив, як і
  просив пункт 4 «Що зробити».
- **`granted_tool_proficiencies` в 2014 — мертві дані.** Рядки виду `{COOKS_UTENSILS: 1}` рушій
  читає через `parseEnumArray(…, ToolCategory)`, тобто чекає **масив**, а на обʼєкті віддає `[]`.
  Тому 2024 писав масивами; форму 2014 не чіпав — це чужа знахідка.

### DDL і порядок кроків для власника

Три числа не мали куди лягти: у `feature` не було жодної колонки про швидкість, ініціативу чи
плоскі хіти. Рішення власника 2026-09-06 — «додати все що треба».
[`db/changes/2026-09-06-kr31.4-feat-mechanics-columns.sql`](../../db/changes/2026-09-06-kr31.4-feat-mechanics-columns.sql):
`speed_bonus INTEGER`, `initiative_proficiency BOOLEAN`, `bonus_hit_points INTEGER`.

**Порядок для власника:**

1. застосувати SQL до робочої бази;
2. `bun run db:pull` — оновить `db/schema.sql` (цей прохід його **не чіпав**: це дамп робочої
   бази, [Р2](../DECISIONS.md#р2));
3. `bun run seed:feat-mechanics-2024:prod` — заллє числа;
4. `bun run seed:remove-asi-feat-2024:prod --apply` — прибере рядок ASI-риси.

У `spells_test` DDL застосований агентом (`./scripts/apply-db-change.sh`), обидва сіди прогнані.

**Власник прогнав усі чотири кроки в проді 2026-09-07.** Перевірено запитом до робочої бази:
`feat` під `RULES_2024` — 74 рядки (ASI-риса прибрана), `feature.gives_ac` — 1 рядок (Оборона),
`speed_bonus` — 2 (Швидкий, Дар швидкості), `initiative_proficiency` — 1 (Пильність),
`bonus_hit_points` — 1 (Дар витривалості). `db/schema.sql` і `prisma/schema.prisma` оновлені
його ж `db:pull`; три колонки в дампі проду на місці.

**Джерело риси виправлено 2026-09-14.** Рядок ASI повертався кожним повним `seed:2024` — двічі
(KR31.2, KR31.8), бо `data/2024/normalized/feats.json` його досі містить, а файл читають ще вісім
місць. Тепер `seedFeats2024` бере риси через `readFeat2024SeedInputs`, який пропускає
`REMOVED_FEAT_NAME_2024` зі скрипта видалення; тест `tests/logic/feat-seed-2024.test.ts` червоний до
фільтра. Клон: видалення, повний `seed:2024:test` (74 риси, 0 помилок), рядка немає,
`ruleset-2024-isolation` 4 / 4. **Прод:** `seed:2024:prod` 2026-09-14 ще старим кодом повернув рядок —
власник прогнав `seed:remove-asi-feat-2024:prod --apply` того ж дня; `creator-content-2024.json` — 74 риси.

**Рядки в `schema.prisma` — відтворення інтроспекції, а не ручний текст.** Перед правкою
`DATABASE_URL=<spells_test> prisma db pull --print` дав рівно три нові поля; після правки
повторний `db pull` у копію дерева не змінив у файлі **жодного байта** (`diff` порожній).
Перейменування snake_case → camelCase — той самий санкціонований виняток
[Р2](../DECISIONS.md#р2), яким уже живе сусідній `infusion.speedBonus @map("speed_bonus")`.

`initiative_proficiency` — булеве, а не число, і це вимір, а не спрощення: `grep` по
`data/2024/srd` і `data/2024/normalized` дає рівно одне джерело числового впливу на ініціативу
(Alert, «add your Proficiency Bonus»); Feral Instinct і Remarkable Athlete дають **перевагу**,
яку лист і так не рахує.

### Що зроблено

| Знахідка | Правка | Тест, доведений червоним |
|---|---|---|
| `L03-feats-01` | `FIGHTING_STYLE_MECHANICS_2024` у `prisma/seed/fightingStyle2024.ts` — жодного рядка коду в рушії | `tests/rules/armor-class.test.ts` (+2), `tests/db/feat-mechanics-2024-seeded.test.ts` |
| `L03-feats-02` | `FEAT_PROFICIENCY_GRANTS_2024` у `prisma/seed/featMechanics2024.ts` | `tests/db/feat-mechanics-2024-seeded.test.ts` — 6 рис |
| `L03-feats-03` | `seedSkillExpertChoiceOptions` — дві групи по 18, як у тієї самої риси 2014 | `tests/db/feat-mechanics-2024-seeded.test.ts` |
| `L03-feats-04` | `feature.speed_bonus` + `sumFeatureSpeedBonus` у `src/rules/feature-stat-grants.ts` | `src/lib/logic/bonus-calculator.test.ts` (+2) |
| `L03-feats-05`, `L09-sheet-derived-04` | `feature.initiative_proficiency` + `findInitiativeProficiencyBonus` | `src/lib/logic/bonus-calculator.test.ts` |
| `L03-feats-08` | `ELEMENTAL_ADEPT` у `UNIQUE_CHOICE_GROUP_BY_FEAT` плюс група «Тип шкоди» в сіді | `tests/rules/repeatable-feats.test.ts` (+1) |
| `L03-feats-12` | `countFeatMasterySlots` у `src/rules/weapon-mastery.ts`; ємність = максимум класів **плюс** слоти рис | `src/lib/components/levelUp/levelup-weapon-mastery.test.ts` (+2) |
| `L03-feats-16` | `prisma/seed/removeAbilityScoreImprovementFeat2024.ts` — умовне видалення з гардом Р28 | `tests/db/feat-mechanics-2024-seeded.test.ts` |
| `L03-feats-17` | нічого — закрито KR27.9 (див. вище) | `src/rules/ability-score-ceiling.test.ts`, наявний |

**Кожен новий тест ламали навмисно й дивилися, що він червоніє.** Чотири приклади:
прибрана `ELEMENTAL_ADEPT` із таблиці повторів — `expected null to deeply equal {…}`;
знята `WEAPON_MASTER: 1` — `expected 4 to be 5` і `expected +0 to be 1`; знятий доданок фіч у
всіх трьох калькуляторах — `expected 30 to be 40`, `expected 35 to be 65`, `expected +0 to be 2`,
`expected 100 to be 140`. Кожен злам знято, файли відновлено.

Дорогою впіймав власну помилку: коментар у `fightingStyle2024.ts` ніс `'` замість `ʼ`, і гейт
`tests/content/ukrainian-apostrophe.test.ts` це показав рядком і номером. Виправлено в джерелі.

### Прогони на кінець проходу

| Прогін | Результат | Проти бази |
|---|---|---|
| `bun run test:no-db` | `2 failed \| 2315 passed (2317)` | ті самі два чужі падіння `radiant-term-sweep`; +22 нові зелені |
| `tests/db/feat-mechanics-2024-seeded.test.ts` (новий гейт) | `20 passed (20)` | новий |
| `tests/rules-2024/multiclass-fifteen.test.ts` | `27 passed (27)` | збігається з базою |
| `tests/rules-2024/acceptance-ten.test.ts` | `3 failed \| 26 passed \| 1 todo` | **чуже червоне, розібрано нижче** |
| решта інтеграційного набору (`--exclude` двох матриць) | `1 failed \| 386 passed (387)` | те саме чуже червоне, іншим файлом |
| `tests/golden/levelup` | `23 passed (23)` | збігається |
| md5 дерева golden | `a6ab512652d916349d3e84431b644602` | **не зрушив** — жоден golden не перегенеровано, дозволу не потребувалося |

`tsc` поза `.next/` чистий; `eslint` по змінених файлах — 0 помилок (24 попередження `any`
успадковані `collectActiveFeatures`, жодного мого рядка).

**Гейт доведено червоним:** `Archery: { bonusToRangedAttackRoll: 3 }` у таблиці сіду при 2 у базі —
`expected { bonusToRangedAttackRoll: 2 } to deeply equal { bonusToRangedAttackRoll: 3 }`. Злам знято.

### Два падіння були мої — і обидва по ділу

Перший повний прогін дав три червоні поза матрицями. Два з них назвали справжній наслідок моєї
роботи, а не поламаний тест:

- **`ruleset-2024-isolation`** чекав 75 рис 2024, а їх 74: рядок ASI-риси прибраний рішенням
  власника. Очікування виправлено на 74 з причиною в коментарі.
- **`choice-option-integrity`** упіймав пʼять моїх опцій «Тип шкоди» як **безрезультатні**: вони
  не несуть ні `effectKind`, ні фічі. Це правда — опція існує, щоб записати вибір гравця й
  обмежити повтор, а сам ефект (ігнорувати опір) не має де працювати, бо опору шкоди в
  застосунку немає. Занесені до `tests/content/known-no-op-choice-options.json` під `BUG-005`,
  поруч із рядками 2014, що стоять там із тієї самої причини.

Третій — **чужий, і він серйозніший за матрицю.**

### Чуже червоне: підкласовий вибір на своєму ж рівні відхиляється

> **Знято 2026-09-07.** Причина — два різні простори id: каталог конструктора несе id робочої
> бази, а фікстура резолвила опцію в `spells_test`, де ту саму опцію сіяли окремо. Рівень — P2,
> шлях гравця самоузгоджений. Розбір і числа —
> [KR31.2](kr31.2-class-choices-2024.md#2026-09-07--обрана-опція-недоступна-на-цьому-рівні-id-каталогу-проти-id-клона).
> Нижче — запис, яким її впіймали.

`08-orc-ranger-guide` не доходить до 5-го рівня: на 3-му серверна дія віддає
`Обрана опція недоступна на цьому рівні` на виборі `Здобич мисливця → Hunter's Prey: Colossus
Slayer`. Два інші падіння матриці (К12 «немає Extra Attack», К13 «немає підкласу») — наслідки
того самого: персонаж застряг на 3-му рівні. Те саме падіння, іншим класом, дає
`tests/actions/repeatable-feats-2024.test.ts`: `BATTLE_MASTER` із «Маневрами майстра бою» на 3-му.

**Чому це не KR31.4:** правиться шлях `validateChoiceSelections(scope: "subclass")` у
`levelup-persistence.ts`, куди не веде жодна моя зміна — я чіпав дані рис, фічі від рис і слот
майстерності. Жодна з десяти фікстур матриці не бере риси взагалі (`feats: null` у всіх десяти).
Опція в базі стоїть правильно: `levels_granted = [3]` при виборі на 3-му рівні, підклас `HUNTER`
у базі один (`subclass_id 151`), дубля немає.

**Звідки взялося:** до 2026-09-06 `subclass_choice_option` під `RULES_2024` була **порожня
цілком** (див. «Виміряний стан» у [README](README.md)). Порожня таблиця означала
`ids.length === 0` і `allowedOptionsAtLevel.length === 0`, тобто валідація мовчки поверталася
`null`, а `subclassChoices` фікстур просто пропускалися. KR31.2 наповнив таблицю того ж дня — і
шлях уперше виконався. Плюс у дереві лежить чужа незакомічена правка самого
`levelup-persistence.ts` (41 рядок додано, 44 знято), що торкається підкласових фіч і кількості
виборів.

**Чому це варто підняти окремо:** якщо симптом такий, як виміряно, то **будь-який персонаж 2024,
що робить підкласовий вибір на рівні, де той дається, зараз не може підвищити рівень**. Це
дорожче за впалу матрицю й належить KR31.2 / KR31.3, а не цій цілі.

### Що НЕ зроблено — і чому

- **`L03-feats-19` (епічні дари) закрито наполовину.** Boon of Fortitude (+40 хітів),
  Boon of Speed (+30 футів) і Boon of Skill (усі 18 навичок плюс експертиза на вибір) працюють.
  **Boon of Energy Resistance і Boon of Truesight — ні, і це не колонка.** Опору шкоди й чуттів
  у персонажа немає **ніде**: `information_schema` знає `damage_resistance` і `senses` лише в
  `creature`, тобто у статблоці НІПа. Дати їх рисі означає завести систему опорів і чуттів
  наскрізь — дані, правило, лист, друк, — а це окрема ціль, не колонка в `feature`.
- **Crafter, Musician, Tavern Brawler** — див. «Готово, коли»: `ChoiceOptionEffectKind` не має
  значення під вибір інструмента, а `WeaponType` — під імпровізовану зброю.
- **Ефект Адепта стихій — окремо від його вибору.** `L03-feats-08` просив вибір типу шкоди, щоб
  повтор було чим обмежити, і це зроблено. Сам ефект — «заклинання обраної стихії ігнорують
  опір» — не працює й не працював у жодній редакції, бо опору шкоди в застосунку немає. Це
  `BUG-005` у [KNOWN-BUGS.md](../KNOWN-BUGS.md); запис перевірено й він лишається чинним, тож
  реєстр цей прохід не правив.
- **Пул зброї для Weapon Master лишається класовим.** Слот риса тепер дає (це і є знахідка), але
  `findWeaponMasteryOptionsForClasses` будує список із `class.weaponProficiencies`, а книга каже
  «provided you have proficiency with it» — тобто мала б рахувати й володіння від рис і виду.
  Персонаж без класової майстерності бачить пул своїх класових володінь; чарівник із
  Martial Weapon Training бойової зброї в ньому не побачить. Це наступний крок, не цей.

### 2026-09-14 — Дар опору стихіям і пул Weapon Master

Два з хвостів «Що НЕ зроблено» вище розблокувала [KR31.6](kr31.6-sheet-derived.md): відтоді в
`feature` є `damage_resistances`, а лист зводить опори з усіх активних фіч персонажа.

**Дар опору стихіям — без DDL.** Опція риси вже вміла класти персонажу свою фічу (`pers_feature`) і
на створенні, і на підвищенні — так їде список «Посвяченого у магію». `seedEnergyResistanceChoiceOptions`
у `prisma/seed/featMechanics2024.ts` заводить девʼять опцій групи «Тип шкоди» (Acid, Cold, Fire,
Lightning, Necrotic, Poison, Psychic, Radiant, Thunder — рівно перелік книги), кожна з фічею
«Опір Стихіям: …» і одним типом у `damage_resistances`. Скільки обирати, форма рис рахувала
вбудованими в компонент гілками; вони винесені в чисту `src/rules/feat-choice-pick-count.ts` без
зміни поведінки Умільця й Бойового адепта, і Дар дістав «2».

**Пул Weapon Master рахує володіння з усіх джерел.** Слот риси тепер обирає з простої або бойової
зброї з властивістю майстерності, якою персонаж володіє, — клас, мультиклас, вид, риси, фічі
(`calculatePersProficiencies`, той самий збір, що малює лист). Сервер бере володіння із
завантаженого персонажа; майстер підвищення отримує їх готовими в `getLevelUpInfo.weaponProficiency`,
бо його персонаж завантажений вужчим запитом.

**Дорогою знайдено, чому чарівник і раніше бачив просту зброю.** Класи без майстерності несуть у
базі двадцять нулів, а не порожній масив, і перевірка `masteryProgression.length === 0` їх не
відсіювала: пул складався з володінь класу випадково. Тепер клас відсіюється за ємністю на своєму
рівні (`readCapacityAtLevel === 0`), і слот чарівника відкриває саме риса. Гейт на нулі додано в
`tests/rules/coverage/weapon-mastery.test.ts`.

**Гейти, усі доведені червоним:**

| Гейт | Злам | Червоне |
|---|---|---|
| `src/rules/feat-choice-pick-count.test.ts`, 4 | прибрано гілку Дару | `1 ≠ 2` |
| `levelup-weapon-mastery.test.ts`, +2 | пул риси вимкнено | `[] ≠ ["HANDAXE"]` |
| `tests/rules/coverage/weapon-mastery.test.ts`, +1 | стара перевірка довжини | нулі дали пул |
| `tests/db/feat-mechanics-2024-seeded.test.ts`, +1 | звʼязки опцій із фічами видалено в `spells_test` | `false ≠ true` |
| `tests/actions/feat-boon-and-weapon-master-2024.test.ts`, 3 | те саме + пул риси вимкнено | `[] ≠ [COLD, FIRE]`; `0 > 0`; `Set{} ≠ Set{SIMPLE, MARTIAL}` |

Наскрізно через `levelUpCharacter`: воїн 18 → 19 бере Дар із вогнем і холодом — `getPersById` дає
опори `[COLD, FIRE]`; чарівник 3 → 4 бере Weapon Master і кинджал — вибір зберігся, пул лише з
простої зброї; чарівник із Martial Weapon Training бачить у пулі й бойову.

**Знайдено дорогою — сід опорів видів стер би Дар у проді.** `findSenseAndResistanceDrift`
(`prisma/seed/speciesSensesAndResistances.ts`) вважав розбіжністю **будь-яку** фічу з опором поза
реєстром видів, а `seed:species-senses:prod -- --apply` такі обнуляє. Після сіду Дару гейт
`species-senses-and-resistances` почервонів саме на цьому; вибірку звужено до фіч, привʼязаних до
рас, підрас, варіантів і виборів виду, — тієї самої, якою гейт міряє корпус. Прод-крок KR31.6 від
власника тепер безпечний для опорів від рис.

**Прод і каталог.** `bun run seed:feat-mechanics-2024:prod` прогнано агентом 2026-09-14 (upsert
контенту, персонажів із Даром у проді 0, колонки є з KR31.6). Запит після: 9 опцій, кожна з фічею
рівно свого типу. `generate:creator-content` з проду: без урахування `updatedAt` і порядку списків
змінився один запис — `BOON_OF_ENERGY_RESISTANCE`, опцій 6 → 15. Id дев'яти нових опцій у
`spells_test` вирівняно з продом (5278–5286; сід бере рядок за `optionNameEng`, тож id тримається,
послідовність підтягнуто), бо сторож `creator-catalog-ids-resolve` назвав саме їх. Каталог із
`spells_test`, як радить KR31.2, цього разу не брався: клон відстає від проду значно ширше, ніж цей
прохід (риси людини 2014, фічі класів), і перезняття затягло б чужі розбіжності.

**Побічно — храповик KR32.1.** `corpus-term-sweep` чекав 32 зняті форми в
`creator-content-2014.json`, а бачив 26: KR33.7 замінив шість самописних описів підкласів 2014 із
«чари» й «ушкоджень». Перевірено підміною носія версією з HEAD — там гейт зелений; храповик
опущено до 26. `radiant-term-sweep` червоний і на HEAD — носії-каталоги дзеркалять прод, це не цей
прохід.

**Лишається відкритим:**

- ~~**Дар істинного зору** і **Crafter / Musician**~~ — закрито того ж дня без DDL, див. нижче.
- **Tavern Brawler** — імпровізованої зброї в `WeaponType` немає.
- **Зміна обраних опорів після довгого відпочинку** — книга дозволяє, лист поки ні: вибір
  фіксується на підвищенні рівня.
- **Weapon Master, взятий на цьому ж підвищенні,** ще не відкриває кроку майстерності в майстрі:
  ємність рахується з уже наявних рис. Набір можна задати на листі будь-коли.

### 2026-09-14 — Істинний зір і інструменти на вибір: рішення власника

Власник відхилив DDL для обох: «ми не будемо робити true sight як дарквіжн, максимум — у списку
фіч» і «вибір трьох інструментів має бути просто записаний у відповідне текстове поле з можливістю
вибрати прямо на чарнику як з мовами, не кроком в білдері».

- **Дар істинного зору** дає рису «Дар істинного зору: істинний зір» (`featStatGrants2024.ts`,
  `grantsFeature`), яку лист показує у списку фіч. Колонки чуття немає й не буде. Маркер — парна
  форма `{{Істинний зір|Truesight}}`: гейт `glossary-marker` вимагає її, бо українських слів два.
- **Ремісник і Музикант** дописують у текст володінь «Інструменти на вибір (3)» — і на створенні
  (риса походження), і на підвищенні. Число — `src/rules/feat-tool-choices.ts`, рядок —
  наявний `formatToolProficiencies(tools, chooseCount)`.
- **Лист.** Біля «Володіння» — «Обрати інструменти»: той самий діалог, що й «Показати мови».
  Діалог мов винесено в `TermPickerDialog` і тепер ділиться; інструменти, уже згадані в тексті,
  позначені, обрані дописуються рядком «Інструменти: …» без повторів
  (`appendToolProficiencies`). Попутно діалог мов бере позначки з поточного тексту, а не з того,
  що був при відкритті листа. `MainStatsSlide.tsx` скоротився 1 185 → 1 155.

**Гейти, доведені червоним:** юніт хелперів і правила (+4, злам — без відсіву й без CRAFTER/MUSICIAN);
`feat-mechanics-2024-seeded` +1 (звʼязок риси з фічею видалено в `spells_test`); наскрізно —
воїн 3 → 4 бере Ремісника, у `customProficiencies` зʼявляється рядок (злам правила — порожньо).

**Браузер** (копія дерева на `spells_test`, :3100, підроблена сесія): «Ковальські інструменти» з
тексту позначені в діалозі; Лютня й Флейта Пана дописалися рядком «Інструменти: Лютня, Флейта
Пана»; мови — «Загальна» позначена, додана «Ельфійська» дала «Загальна, Ельфійська».

**Прод:** `seed:feat-mechanics-2024:prod` повторно, запит — у Дару рівно одна фіча з парним
маркером; `generate:creator-content` з проду змінив лише `BOON_OF_TRUESIGHT.grantsFeature`.
