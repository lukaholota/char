"use client";

import { Button } from "@/components/ui/Button";

export const CharacterCreateHeader = () => {
  return (
    <div>
      <h1>Створити персонажа</h1>
      <Button title="saske" onClick={ () => console.log(2) }/>
    </div>
  );
};