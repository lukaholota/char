/**
 * Доступ до контенту й правил PHB 2024.
 *
 * Передрелізний гейт знято 2026-08-28 — реформа виходить назагал, і акаунт більше не
 * перевіряється. Функція лишається однією точкою на випадок, якщо колись знадобиться
 * знову звузити доступ; каталоги 2024 її вже не питають і тому рендеряться статично.
 */
export function isRules2024Allowed(): boolean {
  return true;
}
