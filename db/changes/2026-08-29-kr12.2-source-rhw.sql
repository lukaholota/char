-- KR12.2 — owner apply only. Не проганяти через Prisma migrate / db push.
-- ЗАСТОСОВАНО власником 2026-08-29. `bun run db:pull` прогнано того ж дня: `RHW` стоїть і в
-- prisma/schema.prisma, і в db/schema.sql. Файл лишається журналом того, що зроблено.
--
-- Питання В у docs/o12-srd-2024-import/questions.md, рішення власника 2026-08-29.
--
-- `RHW` = «Ravenloft: The Horrors Within» — видана книга редакції 2024.
-- `RULES_2024_SOURCES` у scripts/5etools/schema.ts уже визнає її за 2024, а translation.ts уже
-- тримає ратифіковану назву в обох мапах: «Равенлофт: Жахіття Всередині» /
-- «Ravenloft: The Horrors Within». Тобто UI покаже назву одразу — на відміну від `FRAiF`,
-- де назви бракувало (див. 2026-08-24-kr16.3-source-fraif.sql).
--
-- Навіщо: вона тримає останню істоту 2024, якої в каталозі не було, —
--
--   20531  Reanimated Companion
--
-- Це третій із трьох шаблонів виклику з ПС «None (XP 0; PB equals your Proficiency Bonus)».
-- Двоє інших (`Animated Object` 20529, `Giant Insect` 20530) уже стоять у каталозі з 5etools;
-- цей лишався поза ним, бо в маніфесті aidedd він записаний із загубленою літерою —
-- «Ranimated Companion», і звірка за іменем його не знаходила.
--
-- Чому не `UA`: aidedd підписує свою сторінку «Unearthed Arcana 5.5» і показує старішу
-- редакцію статблока (Малий, 4 + 4 × рівень, к6, 2к6 ушкоджень) — чернетку, яка потім вийшла
-- в книзі зміненою (Середній, 5 + 5 × рівень, к8, 2к4). Беремо видану версію.
--
-- Регістр значення такий, як у корпусі 5etools і як уже прийнято в цьому enum для
-- `SatO`, `SCC`, `AitFR_AVT` і `FRAiF` — коди книг тут не переводяться в UPPER_SNAKE.
--
-- ADD VALUE не переписує наявних рядків і не блокує таблицю — застосування безпечне на живій
-- базі. IF NOT EXISTS робить повторний запуск ідемпотентним.
--
ALTER TYPE public."Source" ADD VALUE IF NOT EXISTS 'RHW';
